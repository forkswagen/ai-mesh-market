# Railway: Settings → Root Directory = server (required for monorepo).
# Builder: RAILPACK or DOCKERFILE only (nixpacks no longer supported in config).
# https://docs.railway.app/reference/config-as-code

[build]
builder = "RAILPACK"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 300
